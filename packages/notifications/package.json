{
  "name": "@pulso/notifications",
  "version": "0.1.0",
  "private": true,
  "description": "Email + WhatsApp + Push para notificaciones de Pulso. Stubs preparados para implementación.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pulso/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
