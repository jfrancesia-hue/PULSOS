{
  "name": "@pulso/auth",
  "version": "0.1.0",
  "private": true,
  "description": "RBAC, helpers JWT y password hashing para Pulso.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "@pulso/types": "workspace:*",
    "bcryptjs": "^2.4.3",
    "jose": "^5.9.6",
    "otplib": "^12.0.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.6.3"
  }
}
