-- =====================================================================
--  Pulso · Datos demo extras (recetas, recordatorios, notificaciones)
--  Este script lo ejecuta el seed.ts vía $executeRaw después del seed
--  principal. Es seguro re-correrlo (idempotente para datos demo).
-- =====================================================================

-- Por ahora vacío — los seeds extras se generan en seed.ts via Prisma
-- para mantener integridad referencial.
